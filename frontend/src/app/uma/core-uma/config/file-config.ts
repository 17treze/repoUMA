import { FileConfigModel } from '../models/config/file-config.model';

export const FILE_CONFIG: FileConfigModel = {
    fileExt: '.p7m, .pdf',
    maxSize: 2
}

export const FILE_CONFIG_PDF: FileConfigModel = {
    fileExt: '.pdf',
    maxSize: 2
}
