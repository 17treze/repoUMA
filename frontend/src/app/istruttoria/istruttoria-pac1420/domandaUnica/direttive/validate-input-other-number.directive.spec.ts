import { ValidateInputOtherNumberDirective } from './validate-input-other-number.directive';

describe('ValidateInputOtherNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateInputOtherNumberDirective();
    expect(directive).toBeTruthy();
  });
});
