import { ValidateInputNumberDirective } from './validate-input-number.directive';

describe('ValidateInputNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateInputNumberDirective();
    expect(directive).toBeTruthy();
  });
});
